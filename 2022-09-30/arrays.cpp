/*
Source code provided by the teacher.
As it is presented, it is not completed.
*/

#include <iostream>
#include <cmath>
#include <cstlib>

#define NSIZE 1000

int main() {
	using namespace std;
	int i;
	int n = NSIZE;
	double x[NSIZE], xav, s2, s;
	srand48(time(NULL)); // Seed random number
	
	for (i = 0; i < NSIZE; i++) {
		
	}
	return 0;
}

